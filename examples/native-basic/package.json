{
	"name": "native-basic",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "yarn workspace native-i18n build && vite",
		"build": "yarn workspace native-i18n build && tsc --noEmit && vite build"
	},
	"devDependencies": {
		"typescript": "^5.9.3",
		"vite": "^6.3.5"
	},
	"dependencies": {
		"native-i18n": "workspace:*"
	}
}
